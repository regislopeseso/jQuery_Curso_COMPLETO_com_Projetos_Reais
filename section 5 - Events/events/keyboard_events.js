//! Events: keyboard events
// Evento occorre quando uma tecla qualquer do teclado é pressionada: .keydown()
// $("#form_login [name = username]").keydown(function (event) {
//   alert(`você apertou a tecla ${event.keyCode}`);
// });

// Evento ocorre durante o tempo em que uma tecla é mantida pressionada: .keypress()
// Este evento é similar ao .keydown() porém não detecta algumas teclas tais como o shift, esc, del
// Sua vantagem sobre o keydown é que ele detecta se uma letra pressionada está em maiúscula ou minúscula.
// $("#form_login [name = username]").keypress(function (event) {
//   alert(`você apertou a tecla ${event.keyCode}`);
// });

// Evento occorre quando uma tecla qualquer do teclado é liberada: .keyup()
// faz referência apenas à última tecla utilizada.
// $("#form_login [name = username]").keyup(function (event) {
//   alert(`você apertou a tecla ${event.keyCode}`);
// });
